<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.shopizer</groupId>
	<artifactId>shopizer</artifactId>
	<packaging>pom</packaging>
	<version>3.2.5</version>

	<name>shopizer</name>
	<url>http://www.shopizer.com</url>

	<licenses>
		<license>
			<name>Apache License, Version 2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
		</license>
	</licenses>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- MIGRATION NOTE: Upgraded Spring Boot parent to 4.0.3 for Phase 5 so the runtime moves to the latest stable Spring Boot 4 line without changing application behavior. -->
		<version>4.0.3</version>
	</parent>

	<modules>
		<module>sm-core-model</module>
		<module>sm-core-modules</module>
		<module>sm-core</module>
		<module>sm-shop-model</module>
		<module>sm-shop</module>
	</modules>

	<properties>
			<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
			<!-- MIGRATION NOTE: Centralize the Java 25 test JVM compatibility flags so Surefire and Failsafe can reuse them without relying on implicit argLine state. -->
			<test.jvm.compatibility.args>-javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar --sun-misc-unsafe-memory-access=allow -XX:+EnableDynamicAgentLoading --enable-native-access=ALL-UNNAMED</test.jvm.compatibility.args>

		<!--java version -->
		<!-- MIGRATION NOTE: Raised the compiler release to Java 25 for Phase 5 while preserving the existing source code semantics. -->
		<java.version>25</java.version>

		<maven.compiler.source>${java.version}</maven.compiler.source>
		<maven.compiler.target>${java.version}</maven.compiler.target>

		<shopizer.search.version>2.11.1</shopizer.search.version>
		<shopizer-canadapost.version>2.15.0</shopizer-canadapost.version>

		<!-- TODO replace with starter -->
		<elasticsearch.version>7.5.2</elasticsearch.version>
		<guava.version>27.1-jre</guava.version>
		<commons-lang.version>3.5</commons-lang.version>
		<commons-io.version>2.7</commons-io.version>
		<commons-collections4.version>4.1</commons-collections4.version>
		<commons-validator.version>1.5.1</commons-validator.version>
		<commons-fileupload>1.3.3</commons-fileupload>
		<org.mapstruct.version>1.3.0.Final</org.mapstruct.version>

		<org.apache.httpcomponent.version>4.5.2</org.apache.httpcomponent.version>
		<jakarta.inject.version>2.0.1</jakarta.inject.version>
		<angus-mail.version>2.0.4</angus-mail.version>
		<ehcache2.version>2.10.9.2</ehcache2.version>
		<swagger-annotations.version>1.6.15</swagger-annotations.version>
		<infinispan.version>9.4.18.Final</infinispan.version>
		<infinispan.tree.version>9.4.18.Final</infinispan.tree.version>
		<mysql-jdbc-version>8.0.21</mysql-jdbc-version>
		<oracle.version>18.3.0.0</oracle.version>
		<postgresql.version>42.2.18</postgresql.version>
		<simple-json-version>1.1.1</simple-json-version>
		<!-- MIGRATION NOTE: Align Jackson 2 to the Spring Boot 4 compatibility line so the existing com.fasterxml-based serialization code keeps the same API surface on Boot 4. -->
		<jackson-version-databind>2.20.2</jackson-version-databind>
		<jackson-version>2.20.2</jackson-version>
		<jackson-annotations.version>2.20</jackson-annotations.version>
		<!-- MIGRATION NOTE: Pin H2 to the pre-upgrade test database version to preserve existing schema/bootstrap behavior during the Boot 2.7 transition. -->
		<h2.version>1.4.200</h2.version>
		<geoip2.version>2.7.0</geoip2.version>
		<drools.version>7.32.0.Final</drools.version>
		<google-client-maps-services-version>0.1.6</google-client-maps-services-version>
		<jwt.version>0.8.0</jwt.version>


		<!-- MIGRATION NOTE: Upgraded springdoc to the latest Boot 4 compatible line for Phase 5 while preserving the existing OpenAPI endpoints. -->
		<springdoc.version>3.0.2</springdoc.version>

		<!-- jacoco coverage -->
		<coverage.lines>.30</coverage.lines>
		<coverage.branches>.37</coverage.branches>

	</properties>

	<!--BOM -->
	<dependencyManagement>
		<dependencies>

			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-starter-cache</artifactId>
			</dependency>
			<dependency>
				<groupId>com.shopizer</groupId>
				<artifactId>sm-core</artifactId>
				<version>3.2.5</version>
			</dependency>
			<dependency>
				<groupId>com.shopizer</groupId>
				<artifactId>sm-core-model</artifactId>
				<version>3.2.5</version>
			</dependency>
			<dependency>
				<groupId>com.shopizer</groupId>
				<artifactId>sm-core-modules</artifactId>
				<version>3.2.5</version>
			</dependency>
			<dependency>
				<groupId>com.shopizer</groupId>
				<artifactId>sm-shop-model</artifactId>
				<version>3.2.5</version>
			</dependency>

			<!-- MIGRATION NOTE: javax.inject replaced with jakarta.inject for Spring Boot 3 / Jakarta EE 9 compatibility. -->
			<dependency>
				<groupId>jakarta.inject</groupId>
				<artifactId>jakarta.inject-api</artifactId>
				<version>${jakarta.inject.version}</version>
			</dependency>

			<!-- https://mvnrepository.com/artifact/org.apache.commons/commons-lang3 -->
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-lang3</artifactId>
				<version>${commons-lang.version}</version>
			</dependency>

			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct</artifactId>
				<version>${org.mapstruct.version}</version>
			</dependency>

			<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt</artifactId>
				<version>${jwt.version}</version>
			</dependency>

			<!-- Jackson JSON Processor -->
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-databind</artifactId>
				<version>${jackson-version-databind}</version>
			</dependency>

			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-core</artifactId>
				<version>${jackson-version}</version>
			</dependency>

			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-annotations</artifactId>
				<version>${jackson-annotations.version}</version>
			</dependency>

			<!-- MIGRATION NOTE: javax.mail replaced with Angus Jakarta Mail so existing mail flows stay available on Jakarta-based Spring Boot 3. -->
			<dependency>
				<groupId>org.eclipse.angus</groupId>
				<artifactId>angus-mail</artifactId>
				<version>${angus-mail.version}</version>
			</dependency>

			<!-- http://mvnrepository.com/artifact/com.googlecode.json-simple/json-simple -->
			<dependency>
				<groupId>com.googlecode.json-simple</groupId>
				<artifactId>json-simple</artifactId>
				<version>${simple-json-version}</version>
			</dependency>

			<dependency>
				<!-- MIGRATION NOTE: Retain the legacy MySQL driver coordinates during Phase 2 to avoid artifact-resolution churn while the Jakarta migration is underway. -->
				<groupId>mysql</groupId>
				<artifactId>mysql-connector-java</artifactId>
				<version>${mysql-jdbc-version}</version>
			</dependency>

			<!-- For connecting to oracle -->
			<!-- <dependency> <groupId>com.oracle.database.jdbc</groupId> <artifactId>ojdbc8</artifactId> 
				<version>${oracle.version}</version> </dependency> -->


			<!-- For connecting to postgresql -->
			<!-- <dependency> <groupId>org.postgresql</groupId> <artifactId>postgresql</artifactId> 
				<version>${postgresql.version}</version> <scope>runtime</scope> </dependency> -->

			<!-- Google Map API -->
			<dependency>
				<groupId>com.google.maps</groupId>
				<artifactId>google-maps-services</artifactId>
				<version>${google-client-maps-services-version}</version>
			</dependency>

			<dependency>
				<groupId>org.kie</groupId>
				<artifactId>kie-ci</artifactId>
				<version>${drools.version}</version>
				<exclusions>
					<exclusion>
						<groupId>com.google.guava</groupId>
						<artifactId>guava</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.drools</groupId>
				<artifactId>drools-decisiontables</artifactId>
				<version>${drools.version}</version>
			</dependency>
			<dependency>
				<groupId>org.drools</groupId>
				<artifactId>drools-core</artifactId>
				<version>${drools.version}</version>
			</dependency>
			<dependency>
				<groupId>org.drools</groupId>
				<artifactId>drools-compiler</artifactId>
				<version>${drools.version}</version>
			</dependency>
			<!--spring integration -->
			<dependency>
				<groupId>org.kie</groupId>
				<artifactId>kie-spring</artifactId>
				<version>${drools.version}</version>
			</dependency>
			<!-- end rules engine -->

			<!-- Infinispan -->
			<dependency>
				<groupId>org.infinispan</groupId>
				<artifactId>infinispan-core</artifactId>
				<version>${infinispan.version}</version>
			</dependency>

			<dependency>
				<groupId>org.infinispan</groupId>
				<artifactId>infinispan-cachestore-jdbc</artifactId>
				<version>${infinispan.tree.version}</version>
			</dependency>

			<dependency>
				<groupId>org.infinispan</groupId>
				<artifactId>infinispan-tree</artifactId>
				<version>${infinispan.version}</version>
			</dependency>

			<!-- https://mvnrepository.com/artifact/org.apache.commons/commons-collections4 -->
			<dependency>
				<groupId>org.apache.commons</groupId>
				<artifactId>commons-collections4</artifactId>
				<version>${commons-collections4.version}</version>
			</dependency>


			<!-- https://mvnrepository.com/artifact/commons-validator/commons-validator -->
			<dependency>
				<groupId>commons-validator</groupId>
				<artifactId>commons-validator</artifactId>
				<version>${commons-validator.version}</version>

				<exclusions>
					<exclusion>
						<groupId>commons-collections</groupId>
						<artifactId>commons-collections</artifactId>
					</exclusion>
				</exclusions>
			</dependency>

			<!-- https://mvnrepository.com/artifact/com.amazonaws/aws-java-sdk-s3 -->
			<dependency>
				<groupId>com.amazonaws</groupId>
				<artifactId>aws-java-sdk-s3</artifactId>
				<version>1.11.640</version>
			</dependency>

			<!-- https://mvnrepository.com/artifact/com.amazonaws/aws-java-sdk-ses -->
			<dependency>
				<groupId>com.amazonaws</groupId>
				<artifactId>aws-java-sdk-ses</artifactId>
				<version>1.11.640</version>
			</dependency>

			<!-- google cloud storage -->
			<dependency>
				<groupId>com.google.cloud</groupId>
				<artifactId>google-cloud-storage</artifactId>
				<version>1.74.0</version>
				<exclusions>
					<exclusion>
						<groupId>com.google.guava</groupId>
						<artifactId>guava</artifactId>
					</exclusion>
				</exclusions>
			</dependency>


			<!-- Payment dependencies -->

			<!-- Paypal -->
			<dependency>
				<groupId>com.paypal.sdk</groupId>
				<artifactId>merchantsdk</artifactId>
				<version>2.6.109</version>
			</dependency>

			<!-- Stripe -->
			<dependency>
				<groupId>com.stripe</groupId>
				<artifactId>stripe-java</artifactId>
				<version>19.5.0</version>
			</dependency>

			<!-- Braintree -->
			<dependency>
				<groupId>com.braintreepayments.gateway</groupId>
				<artifactId>braintree-java</artifactId>
				<version>2.73.0</version>
			</dependency>

			<!-- https://mvnrepository.com/artifact/com.maxmind.geoip2/geoip2 -->
			<dependency>
				<groupId>com.maxmind.geoip2</groupId>
				<artifactId>geoip2</artifactId>
				<version>${geoip2.version}</version>
			</dependency>

			<!-- google cloud storage -->
			<!-- https://mvnrepository.com/artifact/com.google.guava/guava -->
			<!-- Up to date for GCP storage issue -->
			<dependency>
				<groupId>com.google.guava</groupId>
				<artifactId>guava</artifactId>
				<version>${guava.version}</version>
			</dependency>

			<dependency>
				<groupId>commons-io</groupId>
				<artifactId>commons-io</artifactId>
				<version>${commons-io.version}</version>
			</dependency>

			<dependency>
				<groupId>commons-fileupload</groupId>
				<artifactId>commons-fileupload</artifactId>
				<version>${commons-fileupload}</version>
			</dependency>

			<dependency>
				<groupId>org.springdoc</groupId>
				<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
				<version>${springdoc.version}</version>
			</dependency>

			<dependency>
				<!-- MIGRATION NOTE: Legacy Swagger annotation types are retained during Phase 2 so controller and DTO metadata can compile unchanged while the documentation runtime moves to springdoc. -->
				<groupId>io.swagger</groupId>
				<artifactId>swagger-annotations</artifactId>
				<version>${swagger-annotations.version}</version>
			</dependency>

		</dependencies>

	</dependencyManagement>

	<!--general dependencies -->
		<dependencies>

		<dependency>
			<groupId>jakarta.inject</groupId>
			<artifactId>jakarta.inject-api</artifactId>
		</dependency>

		<dependency>
			<groupId>jakarta.validation</groupId>
			<artifactId>jakarta.validation-api</artifactId>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.apache.commons/commons-lang3 -->
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
		</dependency>

		<!-- https://mvnrepository.com/artifact/mysql/mysql-connector-java -->

		<!-- Jackson JSON Processor -->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-core</artifactId>
		</dependency>

		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-annotations</artifactId>
		</dependency>

		<!-- https://mvnrepository.com/artifact/net.sf.ehcache/ehcache -->
		<dependency>
			<groupId>net.sf.ehcache</groupId>
			<artifactId>ehcache</artifactId>
			<version>${ehcache2.version}</version>
		</dependency>

		<!-- MIGRATION NOTE: javax.mail-api/mail replaced with the Jakarta Mail implementation for Boot 3 compatibility. -->
		<dependency>
			<groupId>org.eclipse.angus</groupId>
			<artifactId>angus-mail</artifactId>
			<version>${angus-mail.version}</version>
		</dependency>

		<!-- http://mvnrepository.com/artifact/com.googlecode.json-simple/json-simple -->
		<dependency>
			<groupId>com.googlecode.json-simple</groupId>
			<artifactId>json-simple</artifactId>
			<exclusions>
				<exclusion>
					<groupId>junit</groupId>
					<artifactId>junit</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<!-- Elastic search TODO replace with starter-->

		<dependency>
			<groupId>mysql</groupId>
			<artifactId>mysql-connector-java</artifactId>
			<version>${mysql-jdbc-version}</version>
		</dependency>

		<!-- oracle -->
		<!-- <dependency> <groupId>com.oracle</groupId> <artifactId>ojdbc8</artifactId> 
			<version>${oracle.version}</version> </dependency> -->


		<!-- postgres <dependency> <groupId>org.postgresql</groupId> <artifactId>postgresql</artifactId> 
			</dependency> -->

		<dependency>
			<groupId>org.junit.vintage</groupId>
			<artifactId>junit-vintage-engine</artifactId>
			<scope>test</scope>
		</dependency>


	</dependencies>

	<repositories>
		<!-- Sonatype snapshot repo -->
		<repository>
			<id>oss-sonatype</id>
			<name>oss-sonatype</name>
			<url>https://s01.oss.sonatype.org/content/repositories/snapshots/</url>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
		</repository>
		<repository>
			<id>org.jboss.repository.releases</id>
			<name>JBoss Maven Release Repository</name>
			<url>https://repository.jboss.org/nexus/content/repositories/releases</url>
		</repository>
	</repositories>

	<pluginRepositories>
	</pluginRepositories>

	<build>
		<plugins>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.mapstruct</groupId>
							<artifactId>mapstruct-processor</artifactId>
							<version>${org.mapstruct.version}</version>
						</path>
						<!-- other annotation processors -->
					</annotationProcessorPaths>
					<release>${java.version}</release>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- MIGRATION NOTE: Pre-attach the Mockito agent and Java 25 compatibility flags so the existing test suite keeps the same runtime behavior without self-attachment warnings on newer JDKs. -->
					<argLine>${test.jvm.compatibility.args}</argLine>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<configuration>
					<!-- MIGRATION NOTE: Mirror the Surefire Java 25 test JVM flags for future Failsafe runs so integration-test execution stays aligned in CI. -->
					<argLine>${test.jvm.compatibility.args}</argLine>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>3.6.2</version>
				<configuration>
					<rules>
						<!-- MIGRATION NOTE: Enforce the Phase 5 Java 25 toolchain so both CLI and lifecycle-driven CI runs stay on the validated Boot 4 baseline. -->
						<requireJavaVersion>
							<version>[25,)</version>
						</requireJavaVersion>
						<!-- MIGRATION NOTE: Enforce Maven 3.9.11+ consistently across direct enforcer invocations and standard Maven lifecycle runs. -->
						<requireMavenVersion>
							<version>[3.9.11,)</version>
						</requireMavenVersion>
					</rules>
				</configuration>
				<executions>
					<execution>
						<id>phase5-enforce-toolchain</id>
						<goals>
							<goal>enforce</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
				<version>3.1.8</version>
			</plugin>
			<!--OSSRH -->
			<plugin>
				<groupId>org.sonatype.plugins</groupId>
				<artifactId>nexus-staging-maven-plugin</artifactId>
				<version>1.6.7</version>
				<extensions>true</extensions>
				<configuration>
					<serverId>ossrh</serverId>
					<nexusUrl>https://oss.sonatype.org/</nexusUrl>
					<autoReleaseAfterClose>true</autoReleaseAfterClose>
				</configuration>
			</plugin>
			<!--OSSRH -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<version>2.5.3</version>
				<configuration>
					<autoVersionSubmodules>true</autoVersionSubmodules>
					<useReleaseProfile>false</useReleaseProfile>
					<releaseProfiles>release</releaseProfiles>
					<goals>deploy</goals>
				</configuration>
			</plugin>
			<!--OSSRH -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!--OSSRH -->

			<!-- 			
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-gpg-plugin</artifactId>
				<version>1.5</version>
				<executions>
					<execution>
						<id>sign-artifacts</id>
						<phase>verify</phase>
						<goals>
							<goal>sign</goal>
						</goals>
					</execution>
				</executions>
			</plugin> 
			-->

		</plugins>
	</build>

</project>
