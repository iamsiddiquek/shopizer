# Local development defaults. Override with environment variables when needed.
server.host.fallback=${SERVER_HOST_FALLBACK:localhost}
secretKey=${SHOPIZER_SECRET_KEY:ShopizerDevKey01}

# Development-only JWT secret so the app can start without external secret management.
jwt.secret=${JWT_SECRET:shopizer-local-development-jwt-secret-2026-0001}
