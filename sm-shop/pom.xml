<?xml version="1.0"?>

<project
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.shopizer</groupId>
		<artifactId>shopizer</artifactId>
		<version>3.2.5</version>
	</parent>

	<artifactId>sm-shop</artifactId>
	<name>sm-shop</name>
	<url>http://www.shopizer.com</url>

	<properties>
		<coverage.lines>.04</coverage.lines>
		<coverage.branches>.01</coverage.branches>
		<commons-rng-simple.version>1.3</commons-rng-simple.version>
	</properties>

	<dependencies>

		<dependency>
			<groupId>com.shopizer</groupId>
			<artifactId>sm-core</artifactId>
		</dependency>

		<dependency>
			<!-- MIGRATION NOTE: Pin the legacy Caffeine API required by Infinispan 9 for sm-shop integration tests so the web runtime matches the Phase 2 cms/cache compatibility fix in sm-core. -->
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
			<version>2.9.3</version>
		</dependency>

		<dependency>
			<groupId>com.shopizer</groupId>
			<artifactId>sm-core-model</artifactId>
		</dependency>

		<dependency>
			<groupId>com.shopizer</groupId>
			<artifactId>sm-shop-model</artifactId>
		</dependency>

		<!-- Spring boot starters -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<!-- MIGRATION NOTE: Added the explicit Boot 4 MVC starter and excluded Boot's Jackson 3 starter so the existing MVC stack stays on the same com.fasterxml Jackson 2 API surface. -->
			<artifactId>spring-boot-starter-webmvc</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-jackson</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-tomcat</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework</groupId>
			<!-- MIGRATION NOTE: Spring Boot 4 removed the legacy AOP starter, so keep the existing @Aspect behavior with the equivalent Spring AOP dependency. -->
			<artifactId>spring-aop</artifactId>
		</dependency>

		<dependency>
			<groupId>org.aspectj</groupId>
			<!-- MIGRATION NOTE: Spring Boot 4 removed the legacy AOP starter, so keep AspectJ weaving available for the existing @Aspect classes without changing their behavior. -->
			<artifactId>aspectjweaver</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<!-- MIGRATION NOTE: Spring Boot 4 no longer folds bean validation into the dedicated MVC starter, so keep validation explicit to preserve request/body validation behavior. -->
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<dependency>
			<groupId>com.google.guava</groupId>
			<artifactId>guava</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-tomcat</artifactId>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.tomcat.embed</groupId>
			<artifactId>tomcat-embed-jasper</artifactId>
			<scope>provided</scope>
		</dependency>

		<!-- https://mvnrepository.com/artifact/commons-collections/commons-collections -->
		<!-- For Tiles -->
		<dependency>
			<groupId>commons-collections</groupId>
			<artifactId>commons-collections</artifactId>
			<version>3.2.2</version>
		</dependency>

		<!-- Mapstruct -->
		<dependency>
			<groupId>org.mapstruct</groupId>
			<artifactId>mapstruct</artifactId>
		</dependency>

		<!-- H2 session -->
<!--		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
		</dependency>
-->
			<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt</artifactId>
			</dependency>

			<dependency>
				<groupId>org.springframework.boot</groupId>
				<!-- MIGRATION NOTE: Boot 4 defaults to Jackson 3, but this application still uses the existing com.fasterxml Jackson 2 API surface and serialization contracts. -->
				<artifactId>spring-boot-jackson2</artifactId>
			</dependency>

			<dependency>
				<!-- MIGRATION NOTE: Legacy jjwt 0.8.0 still calls javax.xml.bind.DatatypeConverter, so keep the JAXB API on the Boot 3 test/runtime classpath without changing token behavior. -->
				<groupId>javax.xml.bind</groupId>
				<artifactId>jaxb-api</artifactId>
				<version>2.3.1</version>
			</dependency>

				<!-- MIGRATION NOTE: Springfox replaced with springdoc because Spring Boot 3 requires a Jakarta-compatible OpenAPI library. -->
				<dependency>
				<groupId>org.springdoc</groupId>
				<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			</dependency>

			<dependency>
				<groupId>jakarta.inject</groupId>
				<artifactId>jakarta.inject-api</artifactId>
			</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<!-- MIGRATION NOTE: Spring Boot 4 moved TestRestTemplate into spring-boot-resttestclient; keep the existing integration test client behavior unchanged. -->
			<artifactId>spring-boot-resttestclient</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<!-- MIGRATION NOTE: Boot 4 TestRestTemplate auto-configuration now depends on the dedicated restclient module; this keeps the legacy random-port integration tests wiring unchanged. -->
			<artifactId>spring-boot-restclient</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.apache.commons/commons-rng-simple -->
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-rng-simple</artifactId>
			<version>${commons-rng-simple.version}</version>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.owasp.antisamy/antisamy -->
		<!-- xss filter complement -->
		<dependency>
			<groupId>org.owasp.antisamy</groupId>
			<artifactId>antisamy</artifactId>
			<version>1.6.7</version>
		</dependency>

		<!-- https://mvnrepository.com/artifact/org.passay/passay -->
		<!-- Password rules library -->
		<dependency>
			<groupId>org.passay</groupId>
			<artifactId>passay</artifactId>
			<version>1.6.0</version>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
		<finalName>shopizer</finalName>
	</build>

	<packaging>jar</packaging>
</project>
